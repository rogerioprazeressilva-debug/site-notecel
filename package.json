{
  "name": "site-notecel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p public && tailwindcss -i ./input.css -o ./public/output.css --minify && cp index.html script.js public/",
    "dev:css": "tailwindcss -i ./input.css -o ./public/output.css --watch",
    "build:css": "tailwindcss -i ./input.css -o ./public/output.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
